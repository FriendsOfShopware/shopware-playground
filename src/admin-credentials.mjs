/**
 * Single source for the playground's Shopware admin login. The user is
 * provisioned into every version dump by prepare-install.mjs; the shell UI
 * surfaces the same credentials so nobody has to read the installer source.
 */
export const ADMIN_USERNAME = 'admin';
export const ADMIN_PASSWORD = 'Shopware123!';
export const ADMIN_PATH = '/admin';
